####################################################################################
###############################   Audio / Lautstärke   #############################
####################################################################################

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status


####################################################################################
###############################   Fenstersteuerung    ##############################
####################################################################################

# Fokus verschieben
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# Fokus mit Pfeiltasten
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Fenster verschieben
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# Fenster mit Pfeiltasten verschieben
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Layout / Splits / Fullscreen
bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
#bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Floating / Parent
bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+a focus parent
#bindsym $mod+space focus mode_toggle
#bindsym $mod+d focus child

# Fenster schließen
bindsym $mod+Shift+q kill


####################################################################################
###############################      Workspaces       ##############################
####################################################################################

# Workspace-Variablen
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Zu Workspace wechseln
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Fenster auf Workspace verschieben
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10


####################################################################################
###############################   Resize / Modus      ##############################
####################################################################################

# Resize-Modus
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"


####################################################################################
###############################   Programme / Apps    ##############################
####################################################################################

# Terminal & Launcher
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+d exec rofi -show drun
#bindcode $mod+40 exec rofi  -show windo
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# Häufig genutzte Programme (lokal)
bindsym $mod+i exec firefox
bindsym $mod+z exec zotero
bindsym $mod+p  exec positron
bindsym $mod+u  exec --no-startup-id i3-sensible-terminal -e positon 


####################################################################################
###############################        Webapps        ##############################
####################################################################################

# Webanwendungen in Chromium als App
bindsym $mod+Shift+c exec chromium --app="https://calendar.google.com/"
bindsym $mod+a exec chromium --app="https://chatgpt.com/"
bindsym $mod+Shift+u exec chromium --app="https://elearning.uni-oldenburg.de/"

bindsym $mod+y exec chromium --app="https://youtube.com"
bindsym $mod+Mod1+i exec chromium --app="https://www.sprachenlernen24-onlinekurs.de/?a=5690199&b=Italienisch_Komplett_66501332306637694799&c=30103903484&d=999999-pnr130163-tnr2048837&e=205658&f=1&v=1&w=1"

####################################################################################
############################### Skripte / Tmux / Wiki #############################
####################################################################################

# Skripte / Tmux / VimWiki
bindsym $mod+b exec /home/tobias/mydotfiles/i3/.config/i3/select_monitor.sh
bindsym $mod+Shift+b exec xrandr --auto && i3 restart
bindsym $mod+Shift+s exec /home/tobias/mydotfiles/i3/.config/i3/toggle_style.sh
bindsym $mod+t exec --no-startup-id i3-sensible-terminal -e /home/tobias/mydotfiles/i3/.config/i3/tmux-session-rofi.sh
bindsym $mod+w exec --no-startup-id wezterm start --class i3-script-float -- nvim /home/tobias/wiki/index.md 

# Super+Shift+I: Script in WezTerm starten (floating, zentriert)
bindsym $mod+Shift+i exec --no-startup-id wezterm start --class i3-script-float -- /home/tobias/scripts/diary.sh


####################################################################################
###############################   System / Menü       ##############################
####################################################################################

# i3 neu laden / beenden
#bindsym $mod+Shift+?? reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Linux-Menü & Power-Menü
bindsym $mod+space exec /home/tobias/scripts/linux_menue.sh
bindsym $mod+Escape exec /home/tobias/mydotfiles/i3/.config/i3/rofi-power-menu.sh


####################################################################################
###############################     screenshots       ##############################
####################################################################################

# Vollbild -> Datei
bindsym Mod1+Print exec --no-startup-id maim ~/Pictures/screenshot-$(date +%F-%H%M%S).png

# Vollbild -> Clipboard
bindsym Shift+Print exec --no-startup-id maim  | xclip -selection clipboard -t image/png

# Bereich auswählen -> Clipboard
bindsym Print exec --no-startup-id maim -s | xclip -selection clipboard -t image/png

####################################################################################
###############################     bildschrim       ##############################
####################################################################################


bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%-
bindsym XF86MonBrightnessUp   exec --no-startup-id brightnessctl set +5%

bindsym $mod+F5 exec --no-startup-id xsct 6500
bindsym $mod+F6 exec --no-startup-id xsct 3500
